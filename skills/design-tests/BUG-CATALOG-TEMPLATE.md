# Bug Catalog: `<file path>`

**Generated by /design-tests on `<YYYY-MM-DD>`**
**Scope:** `<LOC>` lines, `<count>` public exports, `<count>` IO sites.

## What this file does

One paragraph in plain English. Not a function-by-function recap - the *responsibility* of the module.

## Public interface

| Symbol | Type | Purpose |
|---|---|---|
| `funcA(x: X): Y` | exported function | … |
| `funcB(x: X): Y` | exported function | … |
| `GET /route` | HTTP handler | … |

## IO boundaries

- File system: `<paths read/written>`
- HTTP: `<endpoints called>`
- Time/randomness: `<usage>`

## Bug catalog

| # | Bug (one sentence, failure mode) | Blast radius | Plausibility | Test type | Status |
|---|---|---|---|---|---|
| 1 | … | High/Med/Low - what real thing breaks | Why the code shape makes this a real risk | invariant / round-trip / differential / captured / negative-path / property | open / characterized / fixed |
| 2 | … | … | … | … | open |

## Test plan

For each bug above, the test that catches it:

### Bug #1 - `<short name>`

- **Test type:** `<invariant | round-trip | differential | captured | negative-path | property>`
- **Description string** (will appear in test runner output):
  > `"<file>: <what holds> - guards against <bug>"`
- **What it catches:** specific failure scenario
- **Self-critique:**
  - Could this pass for wrong-but-stable output? `<yes/no, why>`
  - Could this fail under a behavior-preserving refactor? `<yes/no, why>`
  - If property-based: what's the input generator?

### Bug #2 - `<short name>`

…

## Skipped

Bugs considered but deliberately excluded, with reason:

- `<bug X>` - trivial / out of scope / blocked by `<Y>` / deferred until `<condition>`

## Post-write evaluation

*(Fill in after the test suite is written and run.)*

- **Bugs caught** (test failed against current code, fix required): `<list>`
- **Bugs characterized** (test passes; behavior pinned): `<list>`
- **Bugs discovered during writing** not in the original catalog: `<list>`
- **Tests removed** (failed self-critique mid-write): `<list>`

## Notes

- Update this catalog when the file's public interface changes.
- Add a row when a real production bug is found later - that bug becomes a captured-bug test.
